import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <div class="h-screen w-screen bg-black text-white text-xl flex justify-center items-center">
      Hello World!
    </div>
  );
});
